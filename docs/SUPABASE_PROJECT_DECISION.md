# Supabase Project Decision

> HISTORICAL / SUPERSEDED: the prior no-project decision is replaced by the approved staging project below. This is the only Supabase project authorized for current cloud work.

Approved staging project: **JOLI POLI Claw Staging**, ref `fbvzqdqjqcbjopuinknw`, region `ap-southeast-1`.

Supabase schema, Auth, Storage, RPCs, Edge Functions, and the cloud adapter may be implemented and tested there using synthetic staging data. Do not apply work to another project and do not connect the Windows updater project to web business data. Do not migrate any production workbook, real closing, machine/product, employee account, or image without separate authorization.

The accepted local backend remains documented in `LOCAL_BACKEND.md`.
